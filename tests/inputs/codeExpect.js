code.expect(a).to.equal('whatever');
code.expect(a).to.not.equal('whatever');
code.expect(a).to.be.an.object();
code.expect(5).to.be.a.number();
code.expect(a).to.exist();
code.expect(a).to.not.exist();
